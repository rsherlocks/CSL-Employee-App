<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.androidshaper.companyapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".DetailsActivity.EmployeeDetailsActivity"></activity>
        <activity android:name=".PanelActivity.UserPanelActivity" />
        <activity android:name=".ActionViewActivity.AdminToEmployee">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PanelActivity.AdminPanelActivity" />
        <activity android:name=".SignUi.UserRegistration" />
        <activity android:name=".SignUi.UserLogin" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>